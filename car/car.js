const carName = "Volvo";
let maxCarSpeed = "350km/h";
let carOwner = "Anna";

alert("Car model: " + carName);
alert("Max Car Speed: " + maxCarSpeed);
alert("Car Owner: " + carOwner);

carOwner = "Masha";

maxCarSpeed = "400km/h";
alert("Updated Max Car Speed: " + maxCarSpeed);
console.log(`The car's speed has been changed`, maxCarSpeed);

maxCarSpeed = "450km/h";
alert("Updated Max Car Speed: " + maxCarSpeed);
console.log(`The car's speed has been changed`, maxCarSpeed);
